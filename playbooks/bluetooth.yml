---
- name: Bluetooth
  hosts: "*"

  tasks:
    - name: Enable 'bluetooth' service started
      ansible.builtin.systemd:
        name: bluetooth
        enabled: true
        state: started
        masked: false
      become: true
